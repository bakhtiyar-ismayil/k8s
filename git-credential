# Configure Git to use the credential helper for a specific URL
git config credential.helper 'cache --timeout=3600'

# Configure Git to use the credential helper globally
git config --global credential.helper 'store --file ~/.git-credentials'

